# Analytics Engine Dependencies

# Database
sqlalchemy==2.0.23
alembic==1.13.0
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Caching
redis==5.0.1
hiredis==2.2.3

# API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# NOTE (2026-08-28): pandas / numpy / scikit-learn / statsmodels / plotly /
# clickhouse-connect were removed here — none are imported anywhere in app/
# or tests/ (verified by grep). The forecasting in app/services/forecast.py
# is a plain-Python OLS implementation with no ML library. The old pandas
# 2.1.0 / numpy 1.25.0 pins also fail to build on the Python 3.12 CI runner
# (numpy Cython headers require Cython >=3.0.0), which was the actual cause
# of this engine's CI failure. Re-add real, current pins if a genuine
# consumer is introduced later.

# HTTP
httpx==0.25.1
requests==2.31.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Date/Time
python-dateutil==2.8.2
pytz==2023.3

# Logging
loguru==0.7.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
aiosqlite>=0.19.0  # test-only: in-memory DB for the test suite
asgi-lifespan>=2.1.0  # test-only: drives FastAPI startup/shutdown in tests

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
