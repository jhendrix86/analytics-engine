[pytest]
asyncio_mode = auto
# Put the repo root on sys.path so `import app` works when the CI runs the
# bare `pytest` console script (which, unlike `python -m pytest`, does not
# add CWD). Without this, tests/conftest.py fails with ModuleNotFoundError.
pythonpath = .
